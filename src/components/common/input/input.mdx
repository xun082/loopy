import { Meta, Controls, Story } from '@storybook/blocks';
import * as InputStories from './input.stories';

<Meta of={InputStories} />

# Input 输入框

一个可复用的输入框组件，支持多种状态和样式。

## 特性

- 支持所有原生 input 属性
- 错误状态展示
- 可控制宽度
- 支持禁用状态
- 响应式文字大小
- 自定义样式支持

## 使用方法
