import { Meta, Controls, Story } from '@storybook/blocks';
import * as DropdownStories from './dropdown.stories';

<Meta of={DropdownStories} />

# Dropdown 下拉菜单

一个可复用的下拉菜单组件，支持自定义触发器和内容。

## 特性

- 支持自定义触发器和内容
- 点击外部自动关闭
- 可控制点击内容是否关闭
- 支持自定义样式和位置
- 动画过渡效果

## 使用方法
