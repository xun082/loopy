import { Meta, Controls, Story } from '@storybook/blocks';
import * as ButtonStories from './button.stories';

<Meta of={ButtonStories} />

# Button 按钮

一个功能丰富的按钮组件，支持多种样式变体、尺寸和状态。

## 特性

- 6种样式变体
- 4种尺寸选项
- 支持加载状态
- 支持图标（左/右）
- 完全可定制
- 键盘可访问性
- 响应式设计

## 使用方法
